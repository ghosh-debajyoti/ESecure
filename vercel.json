{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf public static && mkdir -p public && cp -R frontend/dist/. public/",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
